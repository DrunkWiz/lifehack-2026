# The app is designed dark. Without this, Streamlit follows the OS setting and
# you get dark cards on a light page with unreadable headings.
[theme]
base = "dark"
primaryColor = "#6fce9a"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#151922"
textColor = "#e6e9ef"

[server]
headless = true
